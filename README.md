# Modo de Uso

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Esta es la base de la tabla de multiplicar  [number] [required]
  -h, --hasta    Limite de la tabla de multiplicar                      [number]
  -l, --listar   Muestra la tabla de multiplicar en consola
                                                      [boolean] [default: false]

```